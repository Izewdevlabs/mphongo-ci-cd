name: CI/CD Config Linter

on:
  push:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Validate workflows
      run: |
        yamllint .github/workflows